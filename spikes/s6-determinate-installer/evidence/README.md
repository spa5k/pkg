# Evidence

Runtime evidence is stored in a private `run.XXXXXX` directory below `/var/tmp/pkg-s6-determinate-installer`.

Each run records the target, expected and actual digests, child arguments, combined output, and exit status.
